{
  "name": "config",
  "description": "Customize the status updates",
  "options": [{
    "name": "get",
    "description": "Show the current configuration",
    "type": 1
  },
  {
    "name": "roles",
    "description": "Ping a role when the status page is updated",
    "type": 2,
    "options": [{
      "name": "add",
      "description": "Add an additional role to ping",
      "type": 1,
      "options": [{
        "name": "role",
        "description": "The role to add",
        "type": 8,
        "required": true
      }]
    },
    {
      "name": "remove",
      "description": "Remove a role currently being pinged",
      "type": 1,
      "options": [{
        "name": "role",
        "description": "The role to remove",
        "type": 8,
        "required": true
      }]
    },
    {
      "name": "get",
      "description": "Show which roles will be pinged",
      "type": 1
    }]
  },
  {
    "name": "mode",
    "description": "Should incident updates edit the original message or send a new message",
    "type": 2,
    "options": [{
      "name": "edit",
      "description": "Edit the original message instead of posting a new message with each incident update.",
      "type": 1
    },
    {
      "name": "post",
      "description": "Post a new message with each incident update",
      "type": 1
    }]
  },
  {
    "name": "username",
    "description": "Change the name of the webhook",
    "type": 2,
    "options": [{
      "name": "set",
      "description": "Set the name of the webhook",
      "type": 1,
      "options": [{
        "name": "name",
        "description": "The new name for the webhook",
        "type": 3,
        "required": true
      }]
    },
    {
      "name": "reset",
      "description": "Use the default username",
      "type": 1
    }]
  },
  {
    "name": "avatar",
    "description": "Change the avatar of the webhook",
    "type": 2,
    "options": [{
      "name": "set",
      "description": "Set the avatar URL for the webhook",
      "type": 1,
      "options": [{
        "name": "name",
        "description": "The avatar URL",
        "type": 3,
        "required": true
      }]
    },
    {
      "name": "reset",
      "description": "Use the default avatar",
      "type": 1
    }]
  }]
}
